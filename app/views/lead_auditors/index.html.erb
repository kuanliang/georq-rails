<div class="col-md-12">
  <div class="service">
    <h1>主任稽核員名單</h1>
    <%= link_to("New service", new_lead_auditor_path, class: "btn btn-primary pull-right") %>
  </div>
  <table class="table table-hover">
    <thead>
      <tr>
        <td>#</td>
        <td>英文名稱</td>
        <td>中文名稱</td>
        <td>EA_CODE</td>
      </tr>
    </thead>
    <tbody>
      <% @lead_auditors.each do |l| %>
        <tr>
          <td>#</td>
          <td><%= l.name_en %></td>
          <td><%= l.name_chi %></td>
          <td><%= "---" %></td>

          <td><%#= service.desctiption %></td>
          <td>        
              <%= link_to("Edit", edit_lead_auditor_path(l), class: "btn btn-sm btn-default")%>
              <%= link_to("Delete", lead_auditor_path(l),    class: "btn btn-sm btn-default", 
                          method: :delete, data: { confirm: "Are you sure?" } )%>
                    </td>
        </tr>
      <% end %>

       <%# @posts.each do |post| %>
      <%# end %>

    </tbody>
  </table>
</div>